{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd legacy && npm run build",
  "outputDirectory": "legacy/dist",
  "framework": "vite",
  "rewrites": [
    { 
      "source": "/(.*)", 
      "destination": "/api/ssr" 
    }
  ]
}
